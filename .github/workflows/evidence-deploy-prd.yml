name: Evidence Deploy Production

on:
  push:
    branches:
      - main
    paths:
      - evidence/**

jobs:
  evidence-deploy-prd:
    uses: ./.github/workflows/evidence-deploy.yml
    with:
      environment: prd
    secrets:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
